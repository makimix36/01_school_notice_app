<% content_for(:title, t('.title')) %>
<div class="container pt-3">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <!-- 検索フォーム -->
#      <%= render 'search_form', q: @q, url: notices_path %>
    </div>
  </div>
  <!-- お知らせ一覧 -->
  <div class="row">
    <div class="col-12">
#     <%= paginate @boards, theme: 'bootstrap-5' %> 
      <div class="row">
        <% if @notices.present? %>
          <%= render @notices %>
        <% else %>
          <div class="mb-3">お知らせがありません</div>
        <% end %>
      </div>
    </div>
  </div>
</div>
